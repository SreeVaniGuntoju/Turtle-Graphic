import turtle

loadWindow = turtle.Screen()
turtle.speed(10)
for i in range(30):
    turtle.circle(5 *i)
    turtle.circle(-5 *i)
    turtle.left(i)
    turtle.right(i)
    turtle.right(90)
    turtle.left(40)

    turtle.color("red")

turtle.exitonclick()
